spring:
  application:
    name: file-service
  cloud:
    config:
      uri: http://localhost:48888
      name: file-service
      profile: development
      label: master
    loadbalancer:
      ribbon:
        enabled: false
